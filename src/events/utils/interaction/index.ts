import * as handleAutocomplete from "./handleAutocomplete";
import * as handleButton from "./handleButton";
import * as handleCommand from "./handleCommand";
import * as handleSelectMenu from "./handleSelectMenu";

export default {
 handleButton: handleButton.default,
 handleSelectMenu: handleSelectMenu.default,
 handleCommand: handleCommand.default,
 handleAutocomplete: handleAutocomplete.default,
};
